const languages = [];
languages[0] = 'html';
languages[1] = 'css';
languages[2] = 'javascript';
languages[3] = 'react';
languages[4] = 'node';
languages[5] = 'postgres';

console.log(languages);

const firstElement = 'The first element of the array is html';
console.log(firstElement);

const thirdElement = 'The third element of the array is javascript';
console.log(thirdElement);

const length = 'The length of the array is ' + languages.length;
console.log(length);

const lastElement = languages[languages.length - 1];
console.log('The last element of the array is ' + lastElement);
